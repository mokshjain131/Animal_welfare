Future Scope:

======================================================================
5. UPGRADE PATH (document these in your README)
======================================================================

  PostgreSQL        →  TimescaleDB (native hypertables + continuous aggregates at scale)
  Recharts          →  D3 (only if a specific custom visualization is needed)
  No cache          →  Redis (add if dashboard response times degrade)
  APScheduler       →  Celery Beat (production-grade task queue)
  Keyword list      →  ML-based relevance classifier (more accurate filtering)
  Word cloud        →  Optional visual alternative for Trending Keywords panel
